name: Deploy to GitHub Pages

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          # Configure these in GitHub: Settings -> Secrets and variables -> Actions -> Variables
          VITE_API_URL: ${{ vars.VITE_API_URL }}
          URL_ONE_MEDIA: ${{ vars.URL_ONE_MEDIA }}
        run: npm run build

      # SPA support for direct links (/login, /app, /app/..., etc.) on GitHub Pages
      - name: Add SPA support (404.html + /app index)
        run: |
          touch dist/.nojekyll

          # Ensure /app (no trailing slash) has a real document on Pages.
          mkdir -p dist/app
          cp dist/index.html dist/app/index.html

          # Keep a custom 404.html if you provided one via /public/404.html.
          if [ ! -f dist/404.html ]; then
            cp dist/index.html dist/404.html
          fi
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
