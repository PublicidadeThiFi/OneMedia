import type { AssistantImportSession } from '../../types/assistant';
export function ImportReprocessCard({session,onRetry}:{session:AssistantImportSession;onRetry:()=>void}){const failed=session.rows.filter(r=>r.status==='failed').length;if(!failed)return null;return <div className="mt-3 rounded-lg border border-red-200 bg-red-50 p-3 text-xs"><b>Reprocessamento disponível</b><p className="mt-1">{failed} falha(s) podem ser selecionadas para nova validação. Sucessos não serão repetidos e uma nova confirmação será exigida.</p><button className="mt-2 rounded border border-red-300 bg-white px-2 py-1 text-red-700" onClick={onRetry}>Preparar somente as falhas</button></div>}
