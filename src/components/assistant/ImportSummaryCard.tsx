import type { AssistantImportSession } from '../../types/assistant';
export function ImportSummaryCard({session}:{session:AssistantImportSession}){const s=session.summary;return <div className="mt-3 rounded-xl border bg-slate-50 p-3 text-xs"><b>Revisão da importação · versão {session.version}</b><div className="mt-2 grid grid-cols-2 gap-1"><span>Detectados: {s.total}</span><span>Selecionados: {s.selected}</span><span>Válidos: {s.valid}</span><span>Com avisos: {s.warnings}</span><span>Inválidos: {s.invalid}</span><span>Duplicidades: {s.possibleDuplicates}</span><span>Sem coordenadas: {s.withoutGeocoding}</span><span>Com imagem: {s.withImage}</span></div><div className="mt-2 text-slate-500">Sessão temporária até {new Date(session.expiresAt).toLocaleTimeString('pt-BR')}.</div></div>}
