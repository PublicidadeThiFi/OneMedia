import type { AssistantDataPoint } from '../../types/assistant';
export function ReservationEligibilityCard({point}:{point:AssistantDataPoint}){return <section className="rounded-xl border border-amber-200 bg-amber-50 p-3" aria-label="Elegibilidade da reserva"><div className="text-xs font-semibold text-amber-900">{point.label}</div><div className="mt-1 text-sm font-medium text-slate-900">{point.value}</div><p className="mt-1 text-xs leading-5 text-amber-800">{point.description}</p><p className="mt-2 text-xs text-slate-500">Nenhuma reserva manual, campanha ou faturamento foi criado.</p></section>}
